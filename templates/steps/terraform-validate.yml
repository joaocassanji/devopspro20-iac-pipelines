parameters:
  - name: tfProjectPath
    type: string

steps: 
    name: tf-validate
  - pwsh: terraform validate
    dependsOn: tf-init
    displayName: Terraform Validate
    workingDirectory: '${{ parameters.tfProjectPath }}'